# Paradigm Bridge - Dependencies
# Install with: pip install -r requirements.txt

requests>=2.25.0     # HTTP communication with Open Ephys GUI

# Optional: ZMQ backend (only needed if using Network Events plugin)
# pip install pyzmq>=22.0.0

# Optional: install PsychoPy separately
# psychopy
